---
phase: 06-retail-order-saga
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified: [src/apps/warehouse-service/internal/worker/order_worker.go, src/apps/warehouse-service/internal/usecase/inventory_usecase.go, src/apps/retail-service/internal/worker/saga_worker.go, src/apps/retail-service/internal/usecase/order_usecase.go]
autonomous: true
requirements: [RR-22, RR-23]
must_haves:
  truths:
    - "Warehouse Service consumes OrderCreated events"
    - "Warehouse reserves stock and emits InventoryReserved/Failed events"
    - "Retail Service consumes Inventory events and updates Order status"
  artifacts:
    - path: "src/apps/warehouse-service/internal/worker/order_worker.go"
      provides: "Consumer for OrderCreated events"
    - path: "src/apps/retail-service/internal/worker/saga_worker.go"
      provides: "Consumer for Saga response events"
  key_links:
    - from: "Warehouse"
      to: "Retail"
      via: "Inventory.Events Topic"
---

<objective>
Implement the first leg of the Saga choreography: Warehouse inventory reservation and feedback to the Retail Service.

Purpose: Coordinate between ordering and inventory to ensure items are available before proceeding.
Output: Inventory reservation logic in Warehouse and Saga response handling in Retail.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-retail-order-saga/RESEARCH.md
@src/apps/warehouse-service/internal/usecase/intake_usecase.go (Reference for inventory logic)
@src/apps/warehouse-service/internal/worker/harvest_worker.go (Reference for consumer)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Inventory Reservation in Warehouse</name>
  <files>src/apps/warehouse-service/internal/usecase/inventory_usecase.go, src/apps/warehouse-service/internal/worker/order_worker.go</files>
  <behavior>
    - Warehouse consumes `OrderCreated` from `order.events`.
    - `InventoryUseCase.ReserveStock` checks stock levels for each item in the order.
    - If stock is sufficient, decrement stock and publish `InventoryReserved`.
    - If stock is insufficient, publish `InventoryReservationFailed`.
    - Use Valkey (distributed lock) for SKU-level locking during reservation (Per Research A1).
  </behavior>
  <action>
    Implement `ReserveStock` logic in `warehouse-service`.
    Create `OrderWorker` in `warehouse-service` to listen to `order.events`.
    Ensure events are published to a new topic `inventory.events`.
  </action>
  <verify>
    <automated>go test ./apps/warehouse-service/internal/usecase/...</automated>
  </verify>
  <done>Warehouse can reserve stock and emit success/fail events.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Saga Response Consumer in Retail</name>
  <files>src/apps/retail-service/internal/worker/saga_worker.go, src/apps/retail-service/internal/usecase/order_usecase.go</files>
  <action>
    Implement `SagaWorker` in `retail-service` to consume events from `inventory.events`.
    On `InventoryReserved`: Update Order status to `PENDING_PAYMENT` (internal state transition).
    On `InventoryReservationFailed`: Update Order status to `CANCELLED` (Saga Rollback per RR-23 Scenario 2).
    Ensure the worker uses an Inbox pattern (idempotency) to handle retried events.
  </action>
  <verify>
    <automated>go build -o /dev/null src/apps/retail-service/internal/worker/saga_worker.go</automated>
  </verify>
  <done>Retail Service handles inventory reservation feedback.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Saga Worker in Retail App</name>
  <files>src/apps/retail-service/internal/app/init.go</files>
  <action>
    Initialize and start `SagaWorker` in the Retail Service.
    Update `cmd/main.go` or `init.go` to include the new Kafka consumer.
  </action>
  <verify>
    <automated>go build -o /dev/null ./apps/retail-service/cmd/main.go</automated>
  </verify>
  <done>Saga worker integrated into Retail Service.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Retail <-> Warehouse | Event-driven boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | Inventory Events | mitigate | Use idempotent processing in Retail (Inbox pattern) to avoid double status updates |
| T-06-03-02 | Denial of Service | Valkey Locking | mitigate | Implement timeouts for Valkey locks to prevent deadlocks if service crashes |
| T-06-03-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Create an order. Verify that Warehouse reserves stock and Retail updates the order status to either PENDING_PAYMENT or CANCELLED (if stock is out).
</verification>

<success_criteria>
Distributed transaction (Saga) correctly coordinates between Retail and Warehouse for stock reservation.
</success_criteria>

<output>
Create `.planning/phases/06-retail-order-saga/06-03-SUMMARY.md` when done
</output>
