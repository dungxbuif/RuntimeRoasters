---
phase: 06-retail-order-saga
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [api/runtime/retail/v1/retail.proto, src/apps/retail-service/internal/domain/order.go, src/apps/retail-service/internal/domain/store.go, src/apps/retail-service/internal/domain/outbox.go, src/apps/retail-service/internal/infrastructure/repository/order_repository.go, src/apps/retail-service/internal/infrastructure/repository/store_repository.go, src/apps/retail-service/internal/usecase/order_usecase.go, src/apps/retail-service/internal/usecase/store_usecase.go, src/apps/retail-service/internal/app/app.go]
autonomous: true
requirements: [RR-21.5, RR-22, RR-23]
must_haves:
  truths:
    - "Retail Service can receive Order requests via gRPC/Connect"
    - "Order creation is atomic with Outbox event storage"
    - "5 retail stores are seeded across Hanoi, HCM, and Da Nang"
    - "Orders are persisted in Postgres with status PENDING"
  artifacts:
    - path: "src/apps/retail-service/internal/domain/order.go"
      provides: "Order domain models and states"
    - path: "src/apps/retail-service/internal/domain/store.go"
      provides: "Retail Store domain models"
    - path: "src/apps/retail-service/internal/infrastructure/repository/order_repository.go"
      provides: "GORM repository for Orders and Outbox"
  key_links:
    - from: "src/apps/retail-service/internal/usecase/order_usecase.go"
      to: "OrderRepository"
      via: "Dependency Injection"
---

<objective>
Initialize the Retail Service foundation, implement Store management with data seeding for 3 major cities, and establish the core Order management logic with Transactional Outbox.

Purpose: Provide a reliable and pre-configured entry point for customer orders.
Output: Retail Service foundation, Store seeding, Order API, and atomic persistence logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-retail-order-saga/RESEARCH.md
@docs/business/sprint6/technical_design.md
@docs/business/sprint6/RR-21.5.md
@docs/business/sprint6/RR-22.md
@docs/business/sprint6/RR-23.md
@.planning/codebase/CONVENTIONS.md
@src/apps/farm-service/internal/domain/outbox_event.go (Reference for Outbox pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Retail API Contracts</name>
  <files>api/runtime/retail/v1/retail.proto</files>
  <action>
    Create `api/runtime/retail/v1/retail.proto` following the pattern in `api/runtime/farm/v1/farm.proto`.
    Define `RetailService` with `CreateOrder`, `GetOrder`, and `ListStores` RPCs.
    Include `Store` message: `id`, `name`, `city`, `address`.
    Include `Order` message with fields: `id`, `store_id`, `items` (repeated Item), `total_amount`, `status`, `created_at`.
    Define `Item` message: `sku`, `quantity`, `price`.
    Run `make proto` in `src/` to generate Go code.
  </action>
  <verify>
    <automated>ls src/runtime/retail/v1/retail.pb.go</automated>
  </verify>
  <done>API contracts defined and Go code generated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Store and Order Domain/Repository</name>
  <files>src/apps/retail-service/internal/domain/store.go, src/apps/retail-service/internal/domain/order.go, src/apps/retail-service/internal/domain/outbox.go, src/apps/retail-service/internal/infrastructure/repository/order_repository.go, src/apps/retail-service/internal/infrastructure/repository/store_repository.go</files>
  <behavior>
    - Define Store and Order GORM models.
    - Order status constants: PENDING, PREPARING, SHIPPING, COMPLETED, REJECTED (per RR-22).
    - Implement `CreateOrderWithOutbox` in `OrderRepository` (atomic transaction).
    - Implement `ListStores` in `StoreRepository`.
  </behavior>
  <action>
    Implement domain models and repositories. Ensure `Order` belongs to `Store`.
    Reuse `OutboxEvent` schema from `farm-service`.
  </action>
  <verify>
    <automated>go test ./apps/retail-service/internal/infrastructure/repository/...</automated>
  </verify>
  <done>Store and Order persistence implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Store Seeding and Service Scaffolding</name>
  <files>src/apps/retail-service/internal/infrastructure/repository/seed.go, src/apps/retail-service/internal/app/init.go, src/apps/retail-service/cmd/main.go</files>
  <action>
    Implement seeding logic for 5 stores in 3 cities (Hanoi, HCM, Da Nang) as defined in `technical_design.md`.
    Scaffold the Retail Service using Clean Architecture.
    Call seeding function during application startup (only if table is empty).
    Implement `OrderUseCase` and `StoreUseCase`.
    Implement gRPC/Connect handlers.
  </action>
  <verify>
    <automated>go build -o /dev/null ./apps/retail-service/cmd/main.go</automated>
  </verify>
  <done>Retail Service scaffolded with seeded store data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Retail Service | External requests enter the system here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | CreateOrder Request | mitigate | Validate inputs (quantities > 0) using domain validation |
| T-06-01-02 | Information Disclosure | ListStores | mitigate | Ensure only public store data is exposed |
</threat_model>

<verification>
Check that `ListStores` returns the 5 seeded stores and `CreateOrder` creates an order linked to a valid `store_id`.
</verification>

<success_criteria>
Retail Service is running, stores are seeded, and orders can be created atomically with outbox events.
</success_criteria>

<output>
Create `.planning/phases/06-retail-order-saga/06-01-SUMMARY.md` when done
</output>
