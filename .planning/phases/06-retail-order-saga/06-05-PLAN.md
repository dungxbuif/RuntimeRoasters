---
phase: 06-retail-order-saga
plan: 05
type: execute
wave: 5
depends_on: [06-04]
files_modified: [src/scripts/simulate_order_saga.go, src/apps/retail-service/internal/usecase/tests/saga_test.go]
autonomous: true
requirements: [RR-22, RR-23]
must_haves:
  truths:
    - "Full Saga flow (Order -> Reservation -> Payment -> Preparing) works end-to-end"
    - "Rollback flow (Order -> Reservation Failed -> Cancelled) works end-to-end"
  artifacts:
    - path: "src/scripts/simulate_order_saga.go"
      provides: "E2E simulation script"
  key_links:
    - from: "Order Simulation"
      to: "Full System"
      via: "Kafka & DB"
---

<objective>
Conduct end-to-end integration testing and verification of the Retail Order Saga, ensuring both happy paths and compensating actions function correctly.

Purpose: Guarantee system reliability and consistency across distributed services.
Output: Simulation script and integration test suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-retail-order-saga/06-01-SUMMARY.md
@.planning/phases/06-retail-order-saga/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Order Saga Simulation Script</name>
  <files>src/scripts/simulate_order_saga.go</files>
  <action>
    Write a Go script in `src/scripts/` that:
    1. Connects to the Retail Service.
    2. Places an order with valid SKUs.
    3. Polls the `GetOrder` API until the status reaches `PREPARING` or timeouts.
    4. Places an order with invalid/out-of-stock SKUs.
    5. Polls until the status reaches `CANCELLED`.
    Use colored terminal output to track the Saga progress.
  </action>
  <verify>
    <automated>go run src/scripts/simulate_order_saga.go</automated>
  </verify>
  <done>Simulation script created and verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Integration Tests for Saga Transitions</name>
  <files>src/apps/retail-service/internal/usecase/tests/saga_test.go</files>
  <behavior>
    - Test Happy Path: Order Created -> Stock Reserved -> Payment Completed -> Status = PREPARING.
    - Test Inventory Fail: Order Created -> Stock Reservation Failed -> Status = CANCELLED.
    - Test Idempotency: Duplicate `InventoryReserved` events do not trigger multiple payment mocks or state errors.
  </behavior>
  <action>
    Implement comprehensive integration tests in `retail-service`. Use a test DB and a mock Kafka if necessary, or run against the actual local environment.
  </action>
  <verify>
    <automated>go test ./apps/retail-service/internal/usecase/tests/...</automated>
  </verify>
  <done>Saga integration tests passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test Script -> System | Verification boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Denial of Service | Simulation Script | accept | Script is for internal testing only; no production impact |
| T-06-05-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Run the simulation script and ensure both success and failure scenarios are correctly handled and reported.
</verification>

<success_criteria>
All integration tests pass and the simulation script confirms the distributed system reaches eventual consistency in all tested scenarios.
</success_criteria>

<output>
Create `.planning/phases/06-retail-order-saga/06-05-SUMMARY.md` when done
</output>
