---
phase: 06-retail-order-saga
plan: 04
type: execute
wave: 4
depends_on: [06-03]
files_modified: [src/apps/retail-service/internal/worker/payment_mock_worker.go, src/apps/retail-service/internal/worker/saga_worker.go, src/apps/retail-service/internal/usecase/order_usecase.go, src/apps/retail-service/internal/delivery/grpc/order_handler.go]
autonomous: true
requirements: [RR-22, RR-23]
must_haves:
  truths:
    - "A mock payment worker simulates payment success/failure"
    - "Retail Service consumes Payment events and updates Order status to PREPARING"
    - "Users can query order status via GetOrder API"
  artifacts:
    - path: "src/apps/retail-service/internal/worker/payment_mock_worker.go"
      provides: "Simulator for Phase 8 Payment Service"
    - path: "src/apps/retail-service/internal/delivery/grpc/order_handler.go"
      provides: "GetOrder API implementation"
  key_links:
    - from: "PaymentMock"
      to: "Retail"
      via: "Payment.Events Topic"
---

<objective>
Mock the Payment service events to complete the Saga flow and implement order status tracking.

Purpose: Validate the full Saga lifecycle before the actual Payment service is built in Phase 8.
Output: Payment mock worker, final state transitions in Retail, and Order status API.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-retail-order-saga/RESEARCH.md
@docs/business/sprint6/RR-22.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Payment Mock Worker</name>
  <files>src/apps/retail-service/internal/worker/payment_mock_worker.go</files>
  <action>
    Create a `PaymentMockWorker` (temporary) that listens to `InventoryReserved` events from `inventory.events`.
    Upon receiving an event, it should wait 1-2 seconds and then publish a `PaymentCompleted` event to `payment.events`.
    This simulates the behavior of the future Payment Service.
  </action>
  <verify>
    <automated>go build -o /dev/null src/apps/retail-service/internal/worker/payment_mock_worker.go</automated>
  </verify>
  <done>Payment simulator implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Handle Payment Success in Retail Saga Worker</name>
  <files>src/apps/retail-service/internal/worker/saga_worker.go, src/apps/retail-service/internal/usecase/order_usecase.go</files>
  <action>
    Update `SagaWorker` to consume events from `payment.events`.
    On `PaymentCompleted`: Update Order status to `PREPARING` (as per RR-22 Scenario 2).
    Ensure proper logging of the state transition.
  </action>
  <verify>
    <automated>go build -o /dev/null src/apps/retail-service/internal/worker/saga_worker.go</automated>
  </verify>
  <done>Retail Service handles payment completion and advances order to PREPARING.</done>
</task>

<task type="auto">
  <name>Task 3: Implement GetOrder API and Wiring</name>
  <files>src/apps/retail-service/internal/delivery/grpc/order_handler.go, src/apps/retail-service/internal/app/init.go</files>
  <action>
    Implement the `GetOrder` RPC in the gRPC handler to allow users to check their order status.
    Wire the `PaymentMockWorker` and update the `SagaWorker` configuration in `init.go`.
  </action>
  <verify>
    <automated>go build -o /dev/null ./apps/retail-service/cmd/main.go</automated>
  </verify>
  <done>Order status tracking API functional and mock flow wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Payment Mock -> Retail | Simulated external boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Information Disclosure | GetOrder | mitigate | Access control: verify `customer_id` in order matches request identity |
| T-06-04-02 | Repudiation | Saga Transitions | mitigate | Audit logging of all status changes in the `orders` table (using `updated_at` and potentially a status log table) |
| T-06-04-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Create an order. Verify it moves from PENDING -> PENDING_PAYMENT -> PREPARING. Query the status via `GetOrder` to confirm.
</verification>

<success_criteria>
Full end-to-end Saga flow is simulated successfully, ending in the PREPARING state.
</success_criteria>

<output>
Create `.planning/phases/06-retail-order-saga/06-04-SUMMARY.md` when done
</output>
