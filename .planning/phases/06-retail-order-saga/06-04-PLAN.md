---
phase: 06-retail-order-saga
plan: 04
type: execute
wave: 4
depends_on: ["06-01", "06-03"]
files_modified: [
  src/apps/retail-service/internal/delivery/grpc/order_handler.go,
  src/apps/retail-service/internal/app/init.go,
  src/apps/retail-service/cmd/main.go,
  deployments/krakend/krakend.json
]
autonomous: true
requirements: [RR-22, RR-23]
must_haves:
  truths:
    - "Retail Service API is accessible via KrakenD Gateway"
    - "Service follows established OpenTelemetry and Logging patterns"
    - "Health checks and Readiness probes are functional"
  artifacts:
    - path: "src/apps/retail-service/internal/delivery/grpc/order_handler.go"
      provides: "gRPC handler for Order operations"
    - path: "deployments/krakend/krakend.json"
      provides: "API Gateway routing for Retail"
  key_links:
    - from: "KrakenD"
      to: "Retail Service"
      via: "gRPC /v1/orders"
---

<objective>
Expose the Retail Service via gRPC and HTTP (through KrakenD Gateway) and finalize the infrastructure integration including telemetry, logging, and health checks, using the shared Outbox Relay.

Purpose: To make the Retail Service available to consumers and ensure its operability within the established ecosystem.
Output: Integrated Retail Service with Gateway routing and observability.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/ARCHITECTURE.md
@src/pkg/base/app.go
@deployments/krakend/krakend.json
@src/pkg/database/outbox/relay.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement gRPC Delivery Handlers</name>
  <files>src/apps/retail-service/internal/delivery/grpc/order_handler.go</files>
  <action>
    - Implement the `RetailService` gRPC handler in `internal/delivery/grpc/order_handler.go`.
    - The handler should map gRPC `CreateOrderRequest` to Domain models and call `OrderUseCase`.
    - Use standard error mapping (per `pkg/errs`).
  </action>
  <verify>
    <automated>go build ./src/apps/retail-service/internal/delivery/grpc/order_handler.go</automated>
  </verify>
  <done>gRPC handlers implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Service Composition with Shared Outbox Relay</name>
  <files>src/apps/retail-service/internal/app/init.go, src/apps/retail-service/cmd/main.go</files>
  <action>
    - Implement `init.go` to perform Manual Dependency Injection.
    - Initialize the shared `outbox.OutboxRelay` (from `src/pkg/database/outbox`) with a 500ms interval (per IN-03).
    - Initialize the `SagaConsumer` worker.
    - Setup `cmd/main.go` to start the `base.App`, run the workers in background, and serve gRPC.
    - Register health checks for DB and Kafka.
  </action>
  <verify>
    <automated>go build ./src/apps/retail-service/cmd/main.go</automated>
  </verify>
  <done>Service bootstrap logic implemented using shared infrastructure.</done>
</task>

<task type="auto">
  <name>Task 3: KrakenD Configuration</name>
  <files>deployments/krakend/krakend.json</files>
  <action>
    - Add a new endpoint `/v1/orders` to `deployments/krakend/krakend.json`.
    - Route it to the `retail-service` gRPC/HTTP endpoint.
    - Ensure appropriate JWT validation (Gate 1) is configured.
  </action>
  <verify>
    <automated>grep "/v1/orders" deployments/krakend/krakend.json</automated>
  </verify>
  <done>API Gateway routing configured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public Internet -> KrakenD | Entry point to the system |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Information Disclosure | gRPC Reflection | mitigate | Disable gRPC reflection in production |
| T-06-07 | Denial of Service | Retail Service | mitigate | Implement rate limiting in KrakenD |
</threat_model>

<verification>
Ensure the service starts without errors and the endpoint is registered in KrakenD.
</verification>

<success_criteria>
- Retail Service is fully integrated into the RuntimeRoasters ecosystem.
- End-to-end flow from API Gateway to DB is functional.
</success_criteria>

<output>
Create .planning/phases/06-retail-order-saga/06-04-SUMMARY.md when done
</output>
