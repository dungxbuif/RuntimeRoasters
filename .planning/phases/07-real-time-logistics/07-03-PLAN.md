---
phase: 07-real-time-logistics
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: [
  src/apps/logistics-service/internal/delivery/grpc/tracking_handler.go,
  src/apps/logistics-service/internal/infrastructure/repository/tracking_repository.go,
  src/apps/logistics-service/internal/infrastructure/event/tracking_publisher.go,
  src/scripts/generate_routes.go,
  src/scripts/simulate_drivers.go,
  src/apps/logistics-service/testdata/routes.json
]
autonomous: true
requirements: [RR-25]
must_haves:
  truths:
    - "Drivers can update their GPS coordinates via API"
    - "GPS updates are stored in Valkey GEO and tracked with a TTL key"
    - "Route generator tool successfully fetches paths from OSRM"
    - "Driver simulator replays actual road paths from routes.json"
  artifacts:
    - path: "src/scripts/generate_routes.go"
      provides: "Tool to fetch and export road routes between POIs using OSRM API"
    - path: "src/apps/logistics-service/testdata/routes.json"
      provides: "Static route data for simulation and UI visualization"
    - path: "src/scripts/simulate_drivers.go"
      provides: "Path-based driver simulator for realistic movement"
  key_links:
    - from: "Driver App (simulated)"
      to: "LogisticsService"
      via: "gRPC UpdateLocation"
    - from: "LogisticsService"
      to: "Kafka"
      via: "tracking publisher (throttled)"
---

<objective>
Enable real-time driver tracking and realistic movement simulation. This includes implementing GPS update APIs, a tool to generate road routes using OSRM, and a path-based simulator that replays these routes.

Purpose: Provide realistic visibility into driver locations and ensure assignment logic works with actual road distances.
Output: Tracking API, Route Generator, and Path-based Simulator.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/pkg/valkey/client.go
@docs/business/product-requirements.md
@docs/business/sprint7/RR-25.md
@.planning/phases/07-real-time-logistics/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GPS Update API with Freshness Tracking</name>
  <files>src/apps/logistics-service/internal/delivery/grpc/tracking_handler.go, src/apps/logistics-service/internal/infrastructure/repository/tracking_repository.go</files>
  <action>
    - Define gRPC service and handler for `UpdateLocation(driver_id, lat, lon)`.
    - Implement `UpdateDriverLocation` in repository:
      1. Use `GEOADD` in Valkey key `drivers:locations`.
      2. Set a separate key `driver:active:<driver_id>` with a TTL of 5 minutes.
    - Ensure `FindNearestAvailableDriver` filters by active drivers.
  </action>
  <verify>
    <automated>go build ./src/apps/logistics-service/internal/delivery/grpc/tracking_handler.go</automated>
  </verify>
  <done>GPS Update API and Valkey storage with TTL-based freshness implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Route Generator Tool (OSRM)</name>
  <files>src/scripts/generate_routes.go</files>
  <action>
    - Create `src/scripts/generate_routes.go` that:
      1. Defines start/end point pairs between seeded Vietnam locations (Farmers -> Roasteries, Roasteries -> Retailers).
      2. Calls the OSRM public API (`http://router.project-osrm.org/route/v1/driving/`) for each pair.
      3. Extracts the coordinate list (polyline) from the response.
      4. Saves the results to `src/apps/logistics-service/testdata/routes.json`.
  </action>
  <verify>
    <automated>go run ./src/scripts/generate_routes.go && ls src/apps/logistics-service/testdata/routes.json</automated>
  </verify>
  <done>Route generator tool implemented and routes exported.</done>
</task>

<task type="auto">
  <name>Task 3: Path-based Driver Simulator</name>
  <files>src/scripts/simulate_drivers.go</files>
  <action>
    - Update `src/scripts/simulate_drivers.go` to:
      1. Load `src/apps/logistics-service/testdata/routes.json`.
      2. For each simulated driver, pick a route and "walk" through the coordinates at a fixed interval (e.g., every 5 seconds).
      3. Call the `UpdateLocation` gRPC API with the current coordinate.
  </action>
  <verify>
    <automated>go build ./src/scripts/simulate_drivers.go</automated>
  </verify>
  <done>Path-based driver simulator updated to use real road routes.</done>
</task>

<task type="auto">
  <name>Task 4: Throttled Kafka Updates</name>
  <files>src/apps/logistics-service/internal/infrastructure/event/tracking_publisher.go</files>
  <action>
    - Implement a producer to publish `logistics.gps.updated` events.
    - Implement throttling: Only publish if 30 seconds have passed or the driver moved > 50m.
  </action>
  <verify>
    <automated>go build ./src/apps/logistics-service/internal/infrastructure/event/tracking_publisher.go</automated>
  </verify>
  <done>Throttled Kafka publisher for GPS updates implemented.</done>
</task>

</tasks>


<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Driver -> Logistics Service | External untrusted input (GPS coordinates) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | GPS Data | mitigate | Validate coordinate ranges; implement rate limiting for GPS updates to prevent DoS. |
| T-07-05 | Information Disclosure | Real-time Locations | mitigate | Ensure GPS updates are only accessible to authorized consumers (e.g., via internal Kafka topics or authenticated APIs). |
</threat_model>

<verification>
Run the simulator, wait for it to stop, and then verify after 5 minutes that `FindNearestAvailableDriver` no longer returns those drivers even if they are still in the GEO set.
</verification>

<success_criteria>
- Driver locations are successfully persisted in Valkey.
- Stale locations are automatically ignored in assignment searches.
- Kafka traffic is minimized via throttling.
- Simulator provides a steady stream of data for testing.
</success_criteria>

<output>
Create .planning/phases/07-real-time-logistics/07-03-SUMMARY.md when done
</output>
