---
phase: 07-real-time-logistics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  src/apps/logistics-service/cmd/main.go,
  src/apps/logistics-service/internal/app/app.go,
  src/apps/logistics-service/migrations/000001_create_logistics_tables.up.sql,
  src/apps/logistics-service/internal/domain/shipment.go,
  src/apps/logistics-service/internal/domain/driver.go,
  src/apps/logistics-service/internal/domain/location.go,
  src/apps/logistics-service/internal/usecase/repository_interfaces.go,
  deployments/logistics-seed.sql
]
autonomous: true
requirements: [RR-25]
must_haves:
  truths:
    - "Logistics service starts without errors"
    - "Database tables for shipments, drivers, and locations are created"
    - "Vietnam seed data (farmers, roasteries, retailers) is inserted"
    - "Valkey client is successfully initialized and connected"
  artifacts:
    - path: "src/apps/logistics-service/migrations/000001_create_logistics_tables.up.sql"
      provides: "Schema for shipments, drivers, vehicles, and locations"
    - path: "src/apps/logistics-service/internal/domain/location.go"
      provides: "Location domain entity for Points of Interest (Farmers, Roasteries, Retailers)"
    - path: "deployments/logistics-seed.sql"
      provides: "Initial seed data for Vietnam locations"
  key_links:
    - from: "LogisticsService"
      to: "Postgres"
      via: "GORM connection"
    - from: "LogisticsService"
      to: "Valkey"
      via: "src/pkg/valkey client"
---

<objective>
Establish the foundation for the Logistics Service, including project scaffolding, database schema for shipments, drivers, and static locations (Farmers, Roasteries, Retailers). Seed the database with the requested Vietnam-based coordinates.

Purpose: Provide the structural base and initial data for real-time logistics and tracking operations.
Output: Scaffolding, DB migrations, domain models, and seed data.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/apps/farm-service/cmd/main.go
@src/pkg/valkey/client.go
@docs/business/sprint7/RR-25.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/07-real-time-logistics/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Logistics Service Scaffolding</name>
  <files>src/apps/logistics-service/cmd/main.go, src/apps/logistics-service/internal/app/app.go, src/apps/logistics-service/config/config.go</files>
  <action>
    - Create the standard service structure in `src/apps/logistics-service`.
    - Initialize the application in `internal/app/app.go`, setting up Logger, Postgres (GORM), and Valkey.
    - Implement configuration loading in `config/config.go` with environment variable support.
    - Use `src/pkg/valkey` for the Valkey client.
  </action>
  <verify>
    <automated>go build ./src/apps/logistics-service/cmd/main.go</automated>
  </verify>
  <done>Service scaffolding complete and compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DB Migrations and Domain Models</name>
  <files>src/apps/logistics-service/migrations/000001_create_logistics_tables.up.sql, src/apps/logistics-service/internal/domain/shipment.go, src/apps/logistics-service/internal/domain/driver.go, src/apps/logistics-service/internal/domain/location.go, src/apps/logistics-service/internal/usecase/repository_interfaces.go</files>
  <behavior>
    - Shipment entity should include: ID, OrderID, DriverID, Status, DestinationAddress, and Timestamps.
    - Driver entity should include: ID, Name, Phone, VehicleID, IsAvailable (bool).
    - Location entity should include: ID, Name, Type (FARM, ROASTERY, RETAILER), Lat, Lng.
    - Database schema should match these entities.
    - Add a `processed_kafka_messages` table for idempotency tracking.
  </behavior>
  <action>
    - Write SQL migrations for `shipments`, `drivers`, `vehicles`, `locations`, and `processed_kafka_messages`.
    - Define Go domain entities in `internal/domain`.
    - Define Repository interfaces in `internal/usecase/repository_interfaces.go`.
  </action>
  <verify>
    <automated>ls src/apps/logistics-service/migrations/*.sql && grep -q "locations" src/apps/logistics-service/migrations/000001_create_logistics_tables.up.sql</automated>
  </verify>
  <done>Domain models and migrations defined. Locations table included.</done>
</task>

<task type="auto">
  <name>Task 3: Seed Vietnam Locations</name>
  <files>deployments/logistics-seed.sql</files>
  <action>
    - Create `deployments/logistics-seed.sql` with the following data:
      - Farmers: Cau Dat (11.895, 108.538), BMT (12.666, 108.038), Pleiku (14.010, 108.040).
      - Roasteries: Song Than (10.880, 106.750), Hoa Lac (21.010, 105.530), Hoa Khanh (16.080, 108.150).
      - Retailers: HCM Q1 (10.776, 106.700), HN Hoan Kiem (21.028, 105.852), DN Hai Chau (16.066, 108.216).
    - Ensure these are inserted into the `locations` table.
  </action>
  <verify>
    <automated>grep -q "FARM_CAU_DAT" deployments/logistics-seed.sql</automated>
  </verify>
  <done>Seed data for Vietnam locations prepared.</done>
</task>

</tasks>


<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service -> Valkey | Credential-protected connection to cache |
| Service -> Postgres | Credential-protected connection to database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | DB/Valkey Credentials | mitigate | Use environment variables and secrets management; do not hardcode. |
| T-07-SC | Tampering | Go modules | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Check service logs for successful DB and Valkey connection on startup.
</verification>

<success_criteria>
- Logistics service compiles and connects to infrastructure.
- Migration files exist and cover required entities.
- Domain types reflect the PRD requirements and include failure states.
- Repository interfaces follow Clean Architecture placement (WR-01).
</success_criteria>

<output>
Create .planning/phases/07-real-time-logistics/07-01-SUMMARY.md when done
</output>
