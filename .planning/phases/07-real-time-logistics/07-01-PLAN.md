---
phase: 07-real-time-logistics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  src/apps/logistics-service/cmd/main.go,
  src/apps/logistics-service/internal/app/app.go,
  src/apps/logistics-service/migrations/000001_create_logistics_tables.up.sql,
  src/apps/logistics-service/internal/domain/shipment.go,
  src/apps/logistics-service/internal/domain/driver.go,
  src/apps/logistics-service/internal/usecase/repository_interfaces.go
]
autonomous: true
requirements: [RR-25]
must_haves:
  truths:
    - "Logistics service starts without errors"
    - "Database tables for shipments and drivers are created"
    - "Valkey client is successfully initialized and connected"
  artifacts:
    - path: "src/apps/logistics-service/migrations/000001_create_logistics_tables.up.sql"
      provides: "Schema for shipments, drivers, and vehicles"
    - path: "src/apps/logistics-service/internal/domain/shipment.go"
      provides: "Shipment domain entity and status enums (including FAILED, CANCELLED per WR-04)"
    - path: "src/apps/logistics-service/internal/usecase/repository_interfaces.go"
      provides: "Repository interfaces in the consumer layer per WR-01"
  key_links:
    - from: "LogisticsService"
      to: "Postgres"
      via: "GORM connection"
    - from: "LogisticsService"
      to: "Valkey"
      via: "src/pkg/valkey client"
---

<objective>
Establish the foundation for the Logistics Service, including project scaffolding, database schema for shipments and drivers, and integration with Valkey for geospatial data.

Purpose: Provide the structural base for real-time logistics and tracking operations.
Output: Scaffolding, DB migrations, and core domain models.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/apps/farm-service/cmd/main.go
@src/pkg/valkey/client.go
@docs/business/sprint7/RR-25.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Logistics Service Scaffolding</name>
  <files>src/apps/logistics-service/cmd/main.go, src/apps/logistics-service/internal/app/app.go, src/apps/logistics-service/config/config.go</files>
  <action>
    - Create the standard service structure in `src/apps/logistics-service`.
    - Initialize the application in `internal/app/app.go`, setting up Logger, Postgres (GORM), and Valkey.
    - Implement configuration loading in `config/config.go` with environment variable support.
    - Use `src/pkg/valkey` for the Valkey client.
  </action>
  <verify>
    <automated>go build ./src/apps/logistics-service/cmd/main.go</automated>
  </verify>
  <done>Service scaffolding complete and compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DB Migrations and Domain Models</name>
  <files>src/apps/logistics-service/migrations/000001_create_logistics_tables.up.sql, src/apps/logistics-service/internal/domain/shipment.go, src/apps/logistics-service/internal/domain/driver.go, src/apps/logistics-service/internal/usecase/repository_interfaces.go</files>
  <behavior>
    - Shipment entity should include: ID, OrderID, DriverID, Status (PENDING, ASSIGNED, PICKED_UP, IN_TRANSIT, DELIVERED, FAILED, CANCELLED), DestinationAddress, and Timestamps. (per WR-04)
    - Driver entity should include: ID, Name, Phone, VehicleID, IsAvailable (bool).
    - Database schema should match these entities.
    - Add a `processed_kafka_messages` table for idempotency tracking (msg_key PRIMARY KEY, topic, processed_at).
  </behavior>
  <action>
    - Write SQL migrations for `shipments`, `drivers`, `vehicles`, and `processed_kafka_messages`.
    - Define Go domain entities in `internal/domain`.
    - Define Repository interfaces in `internal/usecase/repository_interfaces.go` (NOT in domain) per WR-01 and Clean Architecture rules.
  </action>
  <verify>
    <automated>ls src/apps/logistics-service/migrations/*.sql && grep -q "processed_kafka_messages" src/apps/logistics-service/migrations/000001_create_logistics_tables.up.sql</automated>
  </verify>
  <done>Domain models and migrations defined. Interfaces placed in UseCase layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service -> Valkey | Credential-protected connection to cache |
| Service -> Postgres | Credential-protected connection to database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | DB/Valkey Credentials | mitigate | Use environment variables and secrets management; do not hardcode. |
| T-07-SC | Tampering | Go modules | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Check service logs for successful DB and Valkey connection on startup.
</verification>

<success_criteria>
- Logistics service compiles and connects to infrastructure.
- Migration files exist and cover required entities.
- Domain types reflect the PRD requirements and include failure states.
- Repository interfaces follow Clean Architecture placement (WR-01).
</success_criteria>

<output>
Create .planning/phases/07-real-time-logistics/07-01-SUMMARY.md when done
</output>
