---
phase: 06-retail-order-saga
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [
  src/pkg/database/outbox/relay.go,
  src/apps/retail-service/internal/infrastructure/event/publisher.go
]
autonomous: true
requirements: [RR-23]
must_haves:
  truths:
    - "Outbox events are automatically picked up by the Relay"
    - "Events are successfully published to 'retail.order.created' topic (per CR-01)"
    - "Relay marks events as COMPLETED after successful publication"
  artifacts:
    - path: "src/pkg/database/outbox/relay.go"
      provides: "Shared polling worker logic (per WR-03)"
    - path: "src/apps/retail-service/internal/infrastructure/event/publisher.go"
      provides: "Kafka producer implementation"
  key_links:
    - from: "OutboxRelay"
      to: "Kafka"
      via: "EventPublisher.Publish()"
---

<objective>
Refactor the Outbox Relay logic into a shared package and implement the Kafka Publisher for the Retail Service, ensuring reliable asynchronous delivery of Order events to the correct Kafka topics.

Purpose: Guarantee that Every Order created is eventually announced to the rest of the system using standardized infrastructure.
Output: Shared Outbox Relay worker and Retail Kafka Publisher.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/apps/farm-service/internal/infrastructure/event/outbox_relay.go
@docs/business/product-requirements.md
@docs/business/sprint6/RR-23.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Kafka Publisher with PRD Topic Names</name>
  <files>src/apps/retail-service/internal/infrastructure/event/publisher.go</files>
  <action>
    - Implement `KafkaPublisher` in `internal/infrastructure/event/publisher.go`.
    - Map `SupplyOrderCreated` internal event type to `retail.order.created` Kafka topic (per CR-01).
    - Ensure it uses `segmentio/kafka-go` and follows the established pattern.
  </action>
  <verify>
    <automated>go build ./src/apps/retail-service/internal/infrastructure/event/publisher.go</automated>
  </verify>
  <done>Kafka Publisher implemented with correct topic naming.</done>
</task>

<task type="auto">
  <name>Task 2: Shared Outbox Relay Implementation</name>
  <files>src/pkg/database/outbox/relay.go</files>
  <action>
    - Move and generalize the `OutboxRelay` logic from `farm-service` to `src/pkg/database/outbox/relay.go` (per WR-03).
    - It should take an `OutboxRepository` interface and an `EventPublisher` interface.
    - Ensure the polling interval is configurable (per IN-03) with a default of 500ms.
    - Use this shared Relay in `retail-service` initialization (handled in 06-04).
  </action>
  <verify>
    <automated>go build ./src/pkg/database/outbox/relay.go</automated>
  </verify>
  <done>Generic Outbox Relay worker implemented in shared pkg.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OutboxRelay -> Kafka | Local service to trusted message broker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Information Disclosure | Kafka Events | mitigate | Ensure sensitive data is not in event payload; use TLS for Kafka |
| T-06-SC | Tampering | npm/pip/cargo/go installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Verify that the Relay can pick up an event and the Publisher can send it to the 'retail.order.created' topic.
</verification>

<success_criteria>
- Outbox events are successfully relayed to Kafka.
- System maintains eventual consistency between Order DB and Kafka.
- Code duplication is reduced by using shared Outbox logic.
</success_criteria>

<output>
Create .planning/phases/06-retail-order-saga/06-02-SUMMARY.md when done
</output>
