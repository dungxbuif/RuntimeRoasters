# [RR-15.1] Project Scaffolding & Workspace Integration

- **Summary:** Khởi tạo cấu trúc thư mục và tích hợp vào Go Workspace.
- **Status:** `TODO`

## 🛠️ Technical Design
- Tạo thư mục `src/apps/farm-service`.
- Copy cấu trúc `cmd/`, `config/`, `internal/` từ `demo-service`.
- Cập nhật các import path từ `demo-service` thành `farm-service`.
- Tích hợp vào `go.work` (nếu cần, hiện tại đang dùng 1 go.mod duy nhất ở `src/`).
