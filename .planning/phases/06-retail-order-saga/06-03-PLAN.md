---
phase: 06-retail-order-saga
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified: [
  src/apps/retail-service/internal/usecase/order_usecase.go,
  src/apps/retail-service/internal/infrastructure/event/consumer.go
]
autonomous: true
requirements: [RR-22, RR-23]
must_haves:
  truths:
    - "Retail service processes all Saga events including payment and logistics failures (per CR-02)"
    - "Order status transitions logically and handles out-of-order events (per WR-01)"
    - "Event topics align with PRD Section 5.3 (per CR-01)"
  artifacts:
    - path: "src/apps/retail-service/internal/infrastructure/event/consumer.go"
      provides: "Kafka consumer for Saga participants"
  key_links:
    - from: "KafkaConsumer"
      to: "OrderUseCase.HandleEvent()"
      via: "Idempotent event handler"
---

<objective>
Implement the Saga participant logic in the Retail Service with robust state management. This involves listening to PRD-aligned Kafka topics from other services, handling success/failure paths (including logistics failures), and managing out-of-order event transitions.

Purpose: To accurately orchestrate the Order lifecycle across multiple services in a resilient manner.
Output: Robust Saga Consumers and state-machine-aware UseCase logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/business/product-requirements.md
@docs/business/sprint6/RR-22.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Saga Event Consumer with Full Topic Coverage</name>
  <files>src/apps/retail-service/internal/infrastructure/event/consumer.go</files>
  <action>
    - Implement Kafka Consumer in `internal/infrastructure/event/consumer.go`.
    - Subscribe to topics using PRD 5.3 names (per CR-01):
      - `payment.completed`, `payment.failed`
      - `warehouse.stock.reserved`, `warehouse.stock.reserve-failed`
      - `logistics.shipment.assigned`, `logistics.shipment.failed`, `logistics.shipment.delivered` (per CR-02).
    - Ensure `logistics.shipment.failed` is handled.
    - Map events to domain structures and pass to UseCase.
  </action>
  <verify>
    <automated>go build ./src/apps/retail-service/internal/infrastructure/event/consumer.go</automated>
  </verify>
  <done>Saga Consumer implemented with complete PRD-aligned topic coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Robust Order State Machine & Out-of-Order Handling</name>
  <files>src/apps/retail-service/internal/usecase/order_usecase.go</files>
  <behavior>
    - Payment: `OnPaymentCompleted` transitions status to `PAID` (per CR-03).
    - Failure: Any failure event (`payment.failed`, `stock.reserve-failed`, `shipment.failed`) transitions status to `REJECTED`.
    - Robustness: `ShipmentDelivered` should transition status to `COMPLETED` even if `ShipmentAssigned` was missed (per WR-01).
    - Directionality: Transitions must only move to "later" logical states.
  </behavior>
  <action>
    - Extend `OrderUseCase` with `HandleEvent` methods.
    - Implement a "logically later" check for status transitions (per WR-01). Define a numeric priority for statuses: `PENDING(0) < PAID(1) < PREPARING(2) < SHIPPING(3) < COMPLETED(4)`.
    - `REJECTED` is a terminal state.
    - Use `OrderRepository.UpdateStatus` with a conditional update: `UPDATE orders SET status = ? WHERE id = ? AND status < ?` (or similar logic in UseCase).
    - Handle `OnPaymentCompleted` specifically to ensure financial state is tracked (per CR-03).
  </action>
  <verify>
    <automated>go test ./src/apps/retail-service/internal/usecase/...</automated>
  </verify>
  <done>Resilient Order state machine implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kafka -> Retail Consumer | Untrusted events from other services (must be validated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Denial of Service | Saga Consumer | mitigate | Use consumer groups and rate limiting if necessary |
| T-06-05 | Tampering | Saga Events | mitigate | Verify Event Signatures if required; ensure idempotent processing and valid state transitions |
</threat_model>

<verification>
Unit test the `OrderUseCase` with out-of-order event sequences (e.g., Deliver before Assign) to ensure the final state is correct.
</verification>

<success_criteria>
- Retail service acts as a reliable participant in the Order Saga.
- Orders accurately reflect the state of the supply chain even with out-of-order events.
- Compensation paths are fully implemented.
</success_criteria>

<output>
Create .planning/phases/06-retail-order-saga/06-03-SUMMARY.md when done
</output>
