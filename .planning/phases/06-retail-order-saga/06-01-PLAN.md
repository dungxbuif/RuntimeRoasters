---
phase: 06-retail-order-saga
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  src/pkg/database/outbox/outbox.go,
  src/apps/retail-service/cmd/main.go,
  src/apps/retail-service/internal/domain/order.go,
  src/apps/retail-service/internal/usecase/order_usecase.go,
  src/apps/retail-service/internal/infrastructure/repository/order_repository.go,
  api/runtime/retail/v1/retail.proto
]
autonomous: true
requirements: [RR-22, RR-23]
must_haves:
  truths:
    - "Retail service is bootstrapped with Clean Architecture structure"
    - "Orders and OrderItems can be persisted to the database"
    - "CreateOrder operation saves Order and OutboxEvent in a single transaction"
    - "Order model enforces uniqueness on IdempotencyKey (per WR-02)"
  artifacts:
    - path: "src/pkg/database/outbox/outbox.go"
      provides: "Shared Outbox domain models and interfaces (per WR-03)"
    - path: "src/apps/retail-service/internal/domain/order.go"
      provides: "Order and OrderItem entities"
    - path: "src/apps/retail-service/internal/usecase/order_usecase.go"
      provides: "Business logic for order creation"
    - path: "api/runtime/retail/v1/retail.proto"
      provides: "gRPC service definition for Retail"
  key_links:
    - from: "src/apps/retail-service/internal/usecase/order_usecase.go"
      to: "GORM Transaction"
      via: "db.Transaction() block"
---

<objective>
Initialize the Retail Service foundation, refactor the Transactional Outbox pattern into a shared package, define the core domain models for Orders with robust idempotency, and implement the Order creation use case with authorization and validation.

Purpose: To provide a reliable, secure, and standardized entry point for the Order Saga.
Output: Shared Outbox library, bootstrapped Retail Service, and working Order persistence logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/ARCHITECTURE.md
@docs/business/product-requirements.md
@docs/business/sprint6/RR-23.md
@src/apps/farm-service/internal/domain/outbox_event.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor Outbox Pattern and Bootstrap Retail Domain</name>
  <files>src/pkg/database/outbox/outbox.go, src/apps/retail-service/internal/domain/order.go</files>
  <action>
    - Create `src/pkg/database/outbox/outbox.go` (per WR-03). Move `OutboxEvent` struct and `OutboxStatus` constants here from `farm-service` (don't delete from farm-service yet, just copy and generalize).
    - Create standard directory structure for `src/apps/retail-service`.
    - Define `Order` and `OrderItem` structs in `internal/domain/order.go` (per IN-01).
    - `Order` must include: `ID`, `StoreID`, `Status` (`PENDING`, `PAID`, `PREPARING`, `SHIPPING`, `COMPLETED`, `REJECTED`), `TotalAmount`, `Currency`, `IdempotencyKey`.
    - Add `gorm:"uniqueIndex:idx_order_idempotency"` to `IdempotencyKey` field in `Order` struct (per WR-02).
  </action>
  <verify>
    <automated>ls src/pkg/database/outbox/outbox.go && go build ./src/apps/retail-service/internal/domain/...</automated>
  </verify>
  <done>Shared outbox logic established and Retail domain models defined with unique idempotency constraints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Repositories and Secure CreateOrder UseCase</name>
  <files>src/apps/retail-service/internal/infrastructure/repository/order_repository.go, src/apps/retail-service/internal/usecase/order_usecase.go</files>
  <behavior>
    - Validation: Reject if `Quantity <= 0` or `Items` list empty (per IN-02).
    - Auth: Verify `X-User-ID` has `STORE_MGR` role for the target `StoreID` (per WR-04).
    - Idempotency: Gracefully handle GORM unique constraint error on `IdempotencyKey` and return existing order if applicable (per WR-02).
  </behavior>
  <action>
    - Implement `OrderRepository` using GORM.
    - Implement `CreateOrder(ctx, order, items)` in `internal/usecase/order_usecase.go`:
      1. Perform input validation (per IN-02).
      2. Check `StoreID` authorization via context `X-User-Roles` or shared Casbin logic (per WR-04).
      3. Start DB transaction.
      4. Save `Order` and `OrderItems`.
      5. Create `retail.order.created` event (align with PRD 5.3) and save to `OutboxEvent` table using shared models.
      6. Commit transaction.
    - Handle duplicate key errors by checking if an order with the same `IdempotencyKey` already exists.
  </action>
  <verify>
    <automated>go build ./src/apps/retail-service/internal/usecase/...</automated>
  </verify>
  <done>Secure and idempotent Order creation logic implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Define gRPC and Event Contracts</name>
  <files>api/runtime/retail/v1/retail.proto, api/runtime/retail/v1/order_events.proto</files>
  <action>
    - Create `api/runtime/retail/v1/retail.proto` defining `RetailService.CreateOrder`.
    - Create `api/runtime/retail/v1/order_events.proto` for `retail.order.created` event payload (align topic naming with CR-01).
    - Ensure status enums in Proto match Domain (`REJECTED` instead of `CANCELLED`).
  </action>
  <verify>
    <automated>ls api/runtime/retail/v1/*.proto</automated>
  </verify>
  <done>Contracts aligned with PRD and Domain models.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External -> Retail API | Untrusted order requests (IdempotencyKey, StoreID) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Order Creation | mitigate | DB UNIQUE constraint on IdempotencyKey (WR-02) + Input Validation (IN-02) |
| T-06-02 | Spoofing | Retail API | mitigate | Authenticate via JWT + StoreID specific Authorization check (WR-04) |
</threat_model>

<verification>
Check that all models are defined, the UseCase correctly uses transactions, and uniqueness/validation/auth are implemented.
</verification>

<success_criteria>
- Retail service domain and usecase layers are implemented.
- Order creation logic persists to DB and Outbox in one transaction.
- Idempotency and Authorization checks are functional.
- Outbox logic is centralized in `src/pkg`.
</success_criteria>

<output>
Create .planning/phases/06-retail-order-saga/06-01-SUMMARY.md when done
</output>
