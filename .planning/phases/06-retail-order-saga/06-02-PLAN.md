---
phase: 06-retail-order-saga
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [src/apps/retail-service/internal/worker/outbox_relay.go, src/apps/retail-service/internal/infrastructure/event/publisher.go, src/apps/retail-service/internal/app/init.go]
autonomous: true
requirements: [RR-23]
must_haves:
  truths:
    - "Outbox worker picks up PENDING events from the database"
    - "Events are successfully published to the 'order.events' Kafka topic"
    - "Events are marked as PROCESSED in the database after publishing"
  artifacts:
    - path: "src/apps/retail-service/internal/worker/outbox_relay.go"
      provides: "Background worker for event relay"
    - path: "src/apps/retail-service/internal/infrastructure/event/publisher.go"
      provides: "Kafka producer implementation"
  key_links:
    - from: "OutboxRelay"
      to: "Kafka"
      via: "Order.Events Topic"
---

<objective>
Implement the Outbox Relay worker to ensure reliable event publishing from the Retail Service to Kafka.

Purpose: Decouple database transactions from event publishing while guaranteeing "at-least-once" delivery.
Output: Outbox relay worker and Kafka producer integration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-retail-order-saga/RESEARCH.md
@src/apps/farm-service/internal/infrastructure/event/outbox_relay.go (Reference)
@src/pkg/kafka/producer.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Kafka Event Publisher</name>
  <files>src/apps/retail-service/internal/infrastructure/event/publisher.go</files>
  <action>
    Implement `EventPublisher` in `internal/infrastructure/event` using `pkg/kafka`.
    Create a `PublishOrderCreated` method that takes an `Order` and sends a JSON-serialized event to the `order.events` topic.
    Ensure the publisher uses standard project error handling and logging.
  </action>
  <verify>
    <automated>go test ./apps/retail-service/internal/infrastructure/event/...</automated>
  </verify>
  <done>Kafka publisher implemented for order events.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Outbox Relay Worker</name>
  <files>src/apps/retail-service/internal/worker/outbox_relay.go</files>
  <action>
    Implement the `OutboxRelay` worker by porting the logic from `farm-service`.
    The worker should periodically poll the `outbox_events` table for records where `status = 'PENDING'`.
    For each record, use the `EventPublisher` to send it to Kafka, then update the record to `PROCESSED`.
    Use a ticker-based loop with graceful shutdown support.
  </action>
  <verify>
    <automated>go build -o /dev/null src/apps/retail-service/internal/worker/outbox_relay.go</automated>
  </verify>
  <done>Outbox relay worker functional.</done>
</task>

<task type="auto">
  <name>Task 3: Initialize Relay Worker in App</name>
  <files>src/apps/retail-service/internal/app/init.go, src/apps/retail-service/cmd/main.go</files>
  <action>
    Wire the `OutboxRelay` into the application startup in `init.go`.
    Ensure the worker starts in a background goroutine when the main application starts.
    Configure Kafka connection parameters (brokers, topic) via environment variables or config.
  </action>
  <verify>
    <automated>go build -o /dev/null ./apps/retail-service/cmd/main.go</automated>
  </verify>
  <done>Relay worker integrated into the Retail Service lifecycle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Retail DB -> Kafka | Internal event flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Denial of Service | Kafka Producer | mitigate | Implement retries with exponential backoff and circuit breaker in `pkg/kafka` |
| T-06-02-02 | Tampering | Outbox Relay | mitigate | Ensure at-least-once delivery by only marking as PROCESSED after Kafka ACK |
| T-06-02-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Create an order via API, then check Kafka logs (or use a console consumer) to verify that an `OrderCreated` event is published to the `order.events` topic.
</verification>

<success_criteria>
Every order created in the database results in a corresponding event being published to Kafka via the outbox relay.
</success_criteria>

<output>
Create `.planning/phases/06-retail-order-saga/06-02-SUMMARY.md` when done
</output>
